import { NextFunction, Request, Response } from 'express';
import { validator } from '../helpers/validate';
/*
const session = function (req: Request, res: Response, next: NextFunction) {
  const validationRule = {
    "userId",
  }
}*/